# h2 Voileipä
[Tero Karvinen, Palvelinten hallinta 2026 period 4, late spring, h2 Voileipä](https://terokarvinen.com/palvelinten-hallinta/)

## Ympäristö
Virtuaalikone
- Debian 13.4.0 Desktop 64-bit
- 4096MB basememory
- 1 vCPU
- Intel NAT verkkoadapteri
- Oletus selain Firefox

Ansible
- 2.19.4 version

Python
  - 3.13.5 version

## x) Lue ja tiivistä. (Tässä x-alakohdassa ei tarvitse tehdä testejä tietokoneella, vain lukeminen tai kuunteleminen ja tiivistelmä riittää. Tiivistämiseen riittää muutama ranskalainen viiva. Ei siis vaadita pitkää eikä essee-muotoista tiivistelmää. Lisää kuhunkin jokin oma kysymys tai huomio.)
Karvinen 2026: Sudo without password
Munroe 2006: xkcd 149: Sandwitch
Karvinen 2026: Passwordless Sudo with Ansible
Ansiblen sisäänrakennettu dokumentaatio ansible-doc -kommennolla.
Kustakin vain
Johdantokappale (Usein MODULE alla, päättyy OPTIONS alkuun)
Nimetyt optiot selityksineen
Esimerkeistä (EXAMPLES) jokin helppo ja keskeinen esimerkki
'ansible-doc copy': content, dest, src; owner, group, mode;
'ansible-doc apt': name, state, update_cache.
'ansible-doc file': path, recurse, src, state. owner, group, mode;
'ansible-doc user': name; create_home, comment, groups, shell, state, system.
'ansible-doc authorized_key': user, key.


## a) Sudoless. Tee ansiblea varten tunnus, jolla voi käyttää sudoa ilman salasanaa. Sekä ssh-kirjautuminen että sudon käyttö tulee olla ansbilea varten automatisoitu.

### Käyttäjien lisääminen vaatii superuser oikeudet. 
Lisään käyttäjän nimeltä anster seuraavalla komennolla:
```
sudo adduser anster
```
### Annetaan anster käyttäjälle sudo oikeudet lisäämällä se sudoers ryhmään:
```
sudo adduser anster sudo
```
### Nyt lisätään ryhmä jolle annetaan salasanaton sudo oikeus komennolla:
```
sudo groupadd sudoless
```
### Lisätään käyttäjä anster ryhmään adm ja sudoless:
```
sudo adduser anster adm
sudo adduser anster sudoless
```
Anster kuuluu adm ryhmään, näin tehtiin tunnilla (2.4.2026), tämä oli muistinvarainen teko. Miksiköhän näin tehdään?

![]()

### Lisätään sudoers.d määritystiedostoon oikeus salasanattomaan toimintaan ryhmälle sudoless:
```
%sudoless ALL=(ALL) NOPASSWD:ALL
```
```
% tarkoittaa ryhmää, sudoless on ryhmän nimi, ALL tarkoittaa kaikkia käyttäjiä,
(ALL) sallii kaikki toiminnot, NOPASSWD:ALL poistaa kaikilta ryhmän jäseniltä salasana kyselyn.
```

![]()

### (Lisäyksiä 8.4.2026)
Nyt minulla on käyttäjä anster, jolla on oikeudet käyttää sudoa ilman salasanaa. Anster kuuluu ryhmään sudoless. Jos haluan lisää samat oikeudet omaavia käyttäjiä, voin lisätä ne sudoless ryhmään. Aika kiva, mutta Anster joutuu vieläkin näppäillä salasanan kirjautuessaan ssh yhteyden kautta. Annan siis seuraavaksi ansterille SSH avaimen, jottei tarvitse enää nysvätä salasanoja.
```
ssh-copy-id anster@localhost  // tallentaa julkisen avaimen
```

![]()

Tätähän voisi testata ottamalla ssh yhteyden localhost:iin
```
ssh anster@localhost
```
Ja tämähän toimii.

![]()

## b) Antero. Tee salasanaton, automaattisesti ssh:lla kirjautuva tunnus Ansiblella.



## c) Package. Asenna kaksi pakettia ansiblella.

## d) File. Kirjoita orjalle useamman rivin mittainen tiedosto Ansiblella. Määrittele sen omistaja, omistava ryhmä ja oikeudet. Käytä oikeuksille oktaalinumeroa, esim. "0600". Kerro, mitä oikeudet ovat symbolisessa muodossa, esim. "-rwxr--r--". Selitä, mitä kukin käyttäjä saa tehdä tuolle tiedostolle.

## e) Jotain muuta. Näytä esimerkki ansiblen käskystä, jota ei ole vielä käsitelty kurssilla tai kotitehtävissä. Voit ottaa jonkun muun modulin kuin apt, file, copy, user tai authorized_key. Tai voit käyttää ominaisuutta, jota ei vielä ole demonstroitu. Jos tiivistystehtävässä x on mainittu ominaisuuksia, joita ei tunneilla tai läksyissä kokeiltu, nekin kelpaavat.
